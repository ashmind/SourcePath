namespace SourcePath.CSharp {
    public enum SyntaxPathAxis {
        Default,
        Self,
        Child,
        Descendant,
        DescendantOrSelf,
        Parent,
        Ancestor
    }
}